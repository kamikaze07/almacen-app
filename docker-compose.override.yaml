services:
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: almacen-phpmyadmin
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mariadb
    depends_on:
      - mariadb
    networks:
      - almacen-network